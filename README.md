# warehouse_simulator
You will build a text-based simulation of an autonomous warehouse (like an Amazon fulfillment center). You control a robot character (R) on a grid. Your goal is to navigate around walls (#), pick up a package (P), and deliver it to a drop-off zone (D).
